# Dependencies
node_modules/
server/node_modules/

# Build output (rebuilt on deploy)
dist/
server/dist/

# Database (recreated on first run, seeded with default admin)
server/data/

# Logs
*.log
npm-debug.log*
server/logs/

# Env（.env 含本地配置，勿提交；.env.production 保留供生产构建）
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Local tools
cloudflared.exe
public-access.cjs
公网访问.bat
tunnel-server.cjs

# 移交包（重复副本，非部署所需）
业绩管理系统-移交包/

# 旧的前端构建副本（部署时重新构建 app/dist）
server/public/

# Webhook 部署密钥（切勿提交；仅在服务器本地生成）
.webhook_secret

# 本地临时
tmp-users.json
ops/dingtalk-push-yeji/preview-out/

